
clauses
where
select
distinct
order by
limit
offset
group by
having



Count employees in each department
select deptno,count(*) from emp group by deptno;
Find average salary per department
select deptno,avg(sal) from emp group by deptno;
Find maximum salary in each job
select job,max(sal) from emp group by job;
Total salary paid per department
select deptno,sum(sal) from emp group by deptno;

Departments having more than 4 employees

having
it is used with after group by to filter the
record or to keep the condtion with aggregating
functions
select deptno,count(*) from 
          emp group by deptno having count(*)>4;

select job,max(sal) from
          emp group by job having max(sal)<3000;

error- we cannot use where clause after group
select deptno,count(*) from 
          emp group by deptno where count(*)>4;


Average salary by department where avg salary > 2500
select deptno , avg(sal) from
      emp group by deptno having avg(sal)>2500;

Count employees by job, excluding clerks
 select job,count(*) from emp 
            group by job having job<>'clerk';

we can where but it should apply before group by
 select job,count(*) from emp 
           where job<>'clerk' group by job ;


Count employees in each department
         except department 30
select deptno ,count(*) from emp where deptno<>30 group by deptno;

Find average salary of SALESMAN
             in each department
select deptno,avg(sal) from emp where job='salesman' group by deptno;






Count employees hired after 1981 in each department

 select deptno ,count(*) from emp
     where hiredate> "1981-10-10" group by deptno;

query order
select job,count(*) from emp where job<>'clerk' 
   group by job order by job  limit 2 offset 1;